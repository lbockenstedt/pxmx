websockets
psutil
httpx
# mDNS hub auto-discovery (_lm-hub._tcp.local.) at install + runtime; the agent
# imports the vendored src/discovery.py (port-override 8766) which degrades to
# DNS-only if this is missing, but installing it lets the agent hear the hub's
# broadcast and target the hub box's agent listener.
zeroconf>=0.131,<1.0
