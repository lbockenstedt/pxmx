# pxmx — docs

This repo carries a copy of its LM feature page plus the shared topology page:

- [pxmx.md](pxmx.md) — this module's feature reference (purpose, ports, env vars, install flags, commands, gotchas).
- [architecture-topology.md](architecture-topology.md) — the shared LM backbone (hub/spoke/agent mesh, WS+TLS scheme, discovery, signing, onboarding, self-update, state/tenancy).

The **full canonical doc set** (all modules, the env-var reference, the install-flag reference, and the hub/WebUI/generic-agent pages) lives in [`lm/docs/`](../../lm/docs/README.md) — specifically [`lm/docs/pxmx.md`](../../lm/docs/pxmx.md).
